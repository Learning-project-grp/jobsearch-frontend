import ProfileLayout from 'components/Profile/ProfileLayout'
import React from 'react'

type Props = {}

const Education = (props: Props) => {
  return <ProfileLayout>Education</ProfileLayout>
}

export default Education
